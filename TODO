* assert expect
* implement str/ptr/mem support
* get stdout / stderr output
* tests
* fork
* xml / tap: implement show suite, output root suite as top-level node ??
* ensure terminal is left in a sane state on interrupt / signal
* sigaltstack
* block signals upon malloc ? other calls ?
* refine setup/teardown responsibilities (suite vs test) ?
* refine stringification of check API arguments (prevent from expanding args)
